// Makes the jest-dom matchers (toBeInTheDocument, toBeDisabled, …) visible to
// TypeScript / svelte-check in *.test.ts files. The matchers are registered at
// runtime in vitest-setup.ts via `import '@testing-library/jest-dom/vitest'`.
import '@testing-library/jest-dom/vitest';
