// Test stub for SvelteKit's `$app/paths` virtual module.
// Aliased in vite.config.ts under the `test` config.
export const base = '';
export const assets = '';
export const resolveRoute = (path: string): string => path;
